apiVersion: v1
kind: Service
metadata:
  name: docextract-api
  namespace: docextract
spec:
  type: ClusterIP
  selector: { app: docextract, component: api }
  ports:
    - name: http
      port: 80
      targetPort: 8000
